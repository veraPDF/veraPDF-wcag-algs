package logiusAlgorithms.implementation.algorithms;

import logiusAlgorithms.implementation.PDNode;
import logiusAlgorithms.interfaces.Node;

public class SemanticChecksVisitor extends Visitor {
    public void visitNode(Node node) {
        PDNode pdNode = (PDNode) node;
        // iterate over child nodes and apply semantic checks
        
    }
}
